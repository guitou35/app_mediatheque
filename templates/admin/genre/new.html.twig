{% extends 'base.html.twig' %}

{% block title %}New Genre{% endblock %}

{% block body %}
    <div class="container height-full">
        <h1 class="mx-auto text-center text-4xl p-4 text-theme-blue font-semibold">Ajout catégories</h1>
        <div class="md:w-2/4 mx-auto ">
            {{ include('layout/_form.html.twig') }}

            <a href="{{ path('genre_index') }}">back to list</a>
        </div>
    </div>
{% endblock %}
