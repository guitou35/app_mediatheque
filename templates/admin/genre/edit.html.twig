{% extends 'base.html.twig' %}

{% block title %}Editer catégories{% endblock %}

{% block body %}
    <div class="container height-full">
        <h1 class="mx-auto text-center text-4xl p-4 text-theme-blue font-semibold">Editer catégories</h1>
        <div class="md:w-2/4 mx-auto ">
            {{ include('layout/_form.html.twig', {'button_label': 'Update'}) }}
            <div class="flex flex-wrap flex-row items-center justify-center">
                <div class="bttn-blue mx-1 ">
                    <a href="{{ path('genre_index') }}">Retour</a>
                </div>
                {{ include('admin/genre/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
