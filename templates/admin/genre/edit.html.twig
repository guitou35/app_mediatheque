{% extends 'base.html.twig' %}

{% block title %}Edit Genre{% endblock %}

{% block body %}
    <h1>Edit Genre</h1>

    {{ include('layout/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('genre_index') }}">back to list</a>

    {{ include('admin/genre/_delete_form.html.twig') }}
{% endblock %}
